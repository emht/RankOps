import sys
import os

class collect_info(file_name):
	regex = re.compile(
		
	
	def file_read(file_name):
		fp = open(file_name)
		return(contents = fp.read())

	def collect_handles(file_name):
	
	def collect_user_name(file_name):
		
		
